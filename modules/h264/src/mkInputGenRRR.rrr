#include "asim/provides/hasim_common.h"

service MKINPUTGENRRR
{
    server sw (cpp, method) <- hw (bsv, connection) 
    {
       method Initialize     (in UINT64[64] reinit, out UINT64[64] value);  
       method GetInputData   (in UINT64[64] reinit, out UINT64[64] value);
    };

 };
